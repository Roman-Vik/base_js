/*// 1.1 вызов каждую секунду
let timerId = setInterval(()=>console.log("Вызов каждую секунду", ), 1000)*/

/*// 1.2  пример выводит сообщение каждые 2 секунды. Через 5 секунд вывод прекращается:
setTimeout(()=>{
    clearInterval(timerId); alert('Закончить вызов через 5 сек')
}, 5000 )*/

